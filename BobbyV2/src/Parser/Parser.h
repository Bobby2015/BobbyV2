#ifndef PARSER_PARSER_H
#define PARSER_PARSER_H

class Parse
{
	public:
		Parse();
		~Parse();
		int add(unsigned x, unsigned y);

	private:
		unsigned first;
		unsigned second;
}; // Parse

#endif  // PARSER_PARSER_H

#pragma once
