#ifndef PARSER_PARSER_H
#define PARSER_PARSER_H

class Parse
{
	public:
		int add(unsigned x, unsigned y);
		Parse();
		~Parse();
	private:
		unsigned first;
		unsigned second;
}; // Parse

#endif  // PARSER_H

#pragma once
